# Reverse a String

## Problem
Reverse the given string in-place.

## Approach
Use two pointers, one at the beginning and one at the end of the string.
Swap the characters at both positions and move the pointers toward the center.

## Complexity
- Time: O(n)
- Space: O(1)

## Notes
The string is reversed in-place without using another string.